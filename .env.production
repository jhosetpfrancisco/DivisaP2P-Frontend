# URL del backend en producción (Railway). Reemplaza por el dominio real del
# servicio backend, o defínela como build var VITE_API_URL en Railway.
VITE_API_URL=https://REEMPLAZA-con-el-backend.up.railway.app/api
